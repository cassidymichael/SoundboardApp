<Window x:Class="Soundboard.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        SizeToContent="WidthAndHeight"
        MinWidth="400" MaxWidth="500"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="{StaticResource SurfaceBackgroundBrush}"
            BorderBrush="{StaticResource BorderMediumBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0"
                    Background="{StaticResource ElevatedSurfaceBrush}"
                    CornerRadius="8,8,0,0"
                    Padding="16,12"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="{Binding Title}"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource PrimaryTextBrush}"/>
                </Grid>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="24,20">
                <!-- Icon and Message Row -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Warning Icon -->
                    <Viewbox Grid.Column="0"
                             Width="32" Height="32"
                             Margin="0,0,16,0"
                             VerticalAlignment="Top"
                             Visibility="{Binding ShowIcon, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Path Fill="{StaticResource DangerBrush}"
                              Data="M12 2L1 21h22L12 2zm0 3.5L20.5 19h-17L12 5.5zM11 10v4h2v-4h-2zm0 6v2h2v-2h-2z"/>
                    </Viewbox>

                    <!-- Message -->
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="{Binding Message}"
                                   FontSize="13"
                                   Foreground="{StaticResource PrimaryTextBrush}"
                                   TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding SecondaryMessage}"
                                   FontSize="12"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   TextWrapping="Wrap"
                                   Margin="0,12,0,0"
                                   Visibility="{Binding SecondaryMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- Buttons -->
            <Border Grid.Row="2"
                    Background="{StaticResource ElevatedSurfaceBrush}"
                    CornerRadius="0,0,8,8"
                    Padding="16,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{Binding CancelText}"
                            Click="CancelButton_Click"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0,0,8,0"
                            MinWidth="80"
                            Visibility="{Binding ShowCancel, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <Button Content="{Binding ConfirmText}"
                            Click="ConfirmButton_Click"
                            MinWidth="80"
                            Style="{Binding ConfirmButtonStyle}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
